//Take length and breath as input (both integers) and calculate the area of the rectangle
#include<iostream>
using namespace std;
int main(){
    int l,b,A;
    //l=length of rectangle
    //b=breath of rectangle
    //A=area of rectangle
    cout<<"Enter the length of rectangle";
    cin>>l;
    cout<<"Enter the breath of rectangle";
    cin>>b;
    A=l*b;
    cout<<A;
}